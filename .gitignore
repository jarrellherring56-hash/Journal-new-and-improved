# Vercel
.vercel

# Dependencies (none required, but just in case)
node_modules

# Secrets — never commit these; they go in Vercel's Environment Variables
.env
.env.*
*.local
VAPID_KEYS.txt

# Local-only backup, not part of the app
index-neon.html

# Native (Capacitor) build artifacts — regenerated by the build
mobile/www
mobile/ios
mobile/node_modules

# OS / editor cruft
.DS_Store
Thumbs.db
.idea
.vscode
